{
  "name": "@buidl3/cli-pm2",
  "version": "0.0.0",
  "description": "PM2 plugin for Buidl3 CLI",
  "types": "buidl/types/types.d.ts",
  "scripts": {
    "clean-build": "rm -rf ./build",
    "compile": "tsc -p .",
    "build": "npm run clean-build && npm run compile",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "README.md",
    "build",
    "LICENSE"
  ],
  "license": "ISC",
  "dependencies": {
    "gluegun": "^5.1.2",
    "ssh2": "^1.11.0",
    "tar": "^6.1.11"
  },
  "devDependencies": {
    "@types/node": "^18.8.4",
    "@types/ssh2": "^1.11.6",
    "typescript": "^4.8.4"
  }
}
